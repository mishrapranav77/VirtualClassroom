<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.virtualclassroom">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UploadAssignmentActivity"></activity>
        <activity android:name=".LoginTeacherActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".StudentResultsActivity" />
        <activity android:name=".UploadQuizActivity" />
        <activity android:name=".UploadNotesActivity" />
        <activity android:name=".TeacherMenuActivity" />
        <activity android:name=".UploadLectureActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProgressActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".AssignmentActivity" />
        <activity android:name=".QuizActivity" />
        <activity android:name=".NotesActivity" />
        <activity android:name=".LectureActivity" />
        <activity android:name=".StudentMenuActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>